# .env 로 복사한 뒤 값을 채운다. .env 는 git 에 올리지 않는다. 권한: chmod 600 .env
# 마스터 키 생성: echo "sk-$(openssl rand -hex 24)"
LITELLM_MASTER_KEY=

# 1단계 필수
OPENROUTER_API_KEY=

# 발급 후 config.yaml 의 해당 제공처 주석을 해제한다
NVIDIA_NIM_API_KEY=
REQUESTY_API_KEY=
GEMINI_API_KEY=
MISTRAL_API_KEY=
GROQ_API_KEY=
